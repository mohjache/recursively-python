# Recursively unzip and Flatten Files

1. Zipped filed fo inside of imported data.
2. scripts dump all /results into a unique time stamped folder with all file contents lifted to the top, filenames are appended with any child directories to avoid conflicts.
